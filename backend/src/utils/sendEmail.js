export async function sendEmail(to, subject, text) {
    // For now, mock email (prints in terminal)
    console.log("📧 Email to:", to);
    console.log("Subject:", subject);
    console.log("Text:", text);
}